<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khaatabook</title>
    <style>
      /* Global Styling */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      body {
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
        padding: 0 16px;
      }
      /* Navigation Bar */
      nav {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 16px 0;
        font-weight: 500;
        background-color: #ffffff;
        border-bottom: 1px solid #ddd;
        margin-bottom: 24px;
      }
      nav h2 {
        margin: 0;
      }
      nav a {
        color: #007bff;
        text-decoration: none;
        margin-left: 16px;
      }
      nav a:hover {
        text-decoration: underline;
      }
      /* Section and Heading */
      section {
        max-width: 800px;
        margin: 0 auto;
      }
      h1 {
        font-size: 24px;
        color: #1d4ed8;
        font-weight: bold;
        margin-bottom: 16px;
      }
      /* Form Styling */
      form {
        display: flex;
        flex-direction: column;
        gap: 16px;
      }
      label {
        font-weight: bold;
        margin-bottom: 8px;
      }
      .input-field,
      textarea {
        padding: 10px;
        font-size: 16px;
        color: #1d4ed8;
        border: 1px solid #ccc;
        border-radius: 4px;
        width: 100%;
        max-width: 400px;
      }
      textarea {
        height: 128px;
        resize: vertical;
        background-color: #e2e2e2;
      }
      .submit-btn {
        padding: 10px 16px;
        background-color: #1d4ed8;
        color: #fff;
        font-weight: bold;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        max-width: 150px;
      }
      .submit-btn:hover {
        background-color: #2563eb;
      }
    </style>
  </head>

  <body>
    <nav>
      <h2>Khaatabook</h2>
      <div>
        <a href="/">Home</a>
        <a href="/create">Create New Hisaab</a>
      </div>
    </nav>
    <section>
      <h1>Edit Hisaab of <%= fileName %></h1>
      <form action="/update/<%= fileName %>" method="post">
        <label for="newFileName">Edit File Name</label>
        <input
          type="text"
          id="newFileName"
          name="newFileName"
          class="input-field"
          value="<%= fileName %>"
          placeholder="Enter new name"
        />
        
        <label for="fileData">File Content</label>
        <textarea id="fileData" name="fileData"><%= data %></textarea>
        
        <input type="submit" value="Save Changes" class="submit-btn" />
      </form>
    </section>
  </body>
</html>
