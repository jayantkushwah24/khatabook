<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khaatabook</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <style>
      /* General Styles */
      body {
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }
      a {
        text-decoration: none;
        color: #007bff;
      }

      /* Navbar Styles */
      nav {
        padding: 16px 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #ffffff;
        border-bottom: 1px solid #ddd;
        font-weight: 500;
      }
      nav h2 {
        margin: 0;
      }
      nav a {
        margin-left: 16px;
      }

      /* Section Styles */
      section {
        padding: 24px;
      }
      h1 {
        color: #2563eb;
        font-size: 20px;
        font-weight: bold;
        margin: 0 0 20px;
        letter-spacing: -0.5px;
      }

      /* File Item Styles */
      .file-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        max-width: 50%;
        padding: 12px 16px;
        margin-top: 12px;
        background-color: #2563eb;
        color: #ffffff;
        border-radius: 8px;
      }
      .file-item h3 {
        margin: 0;
      }

      /* Icon Links Styles */
      .icon-links i {
        font-size: 18px;
        color: #ffffff;
        margin-left: 8px;
        transition: color 0.3s;
      }
      .icon-links i:hover {
        color: #ff6347;
      }

      /* No Files Message */
      .no-files {
        color: #555;
        font-size: 16px;
        margin-top: 20px;
      }
    </style>
  </head>

  <body>
    <nav>
      <h2>Khaatabook</h2>
      <div>
        <a href="/">Home</a>
        <a href="/create">Create New Hisaab</a>
      </div>
    </nav>
    <section>
      <h1>All Hisaab</h1>

      <!-- Display message if there are no files -->
      <% if (files.length === 0) { %>
      <p class="no-files">No hisaab files found.</p>
      <% } else { %> <% files.forEach((file) => { %>
      <div class="file-item">
        <h3><%= file %></h3>
        <div class="icon-links">
          <a href="/edit/<%= file %>"><i class="ri-edit-line"></i></a>
          <a href="/delete/<%= file %>"><i class="ri-delete-bin-line"></i></a>
        </div>
      </div>
      <% }) %> <% } %>
    </section>
  </body>
</html>
